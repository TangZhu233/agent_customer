# ============================================================
# 智能客服 Agent 项目 — pip 依赖清单
# 安装方式（在 VSCode 终端中执行）：
#   conda activate agent_customer
#   pip install -r requirements.txt
# ============================================================

# LangChain 核心（Agent 框架）
langchain>=1.0.0
langgraph>=1.0.0
langchain-core>=1.0.0
langchain-community>=0.3.0

# DeepSeek 模型集成（LangChain 官方包）
langchain-deepseek>=0.1.0

# OpenAI SDK（DeepSeek API 兼容 OpenAI 接口格式）
openai>=1.0.0

# Web 框架（提供 API 接口）
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 环境变量管理（.env 文件读取）
python-dotenv>=1.0.0

# 数据校验（请求/响应模型）
pydantic>=2.0.0

# ============================================================
# 以下为企业级 RAG 知识问答新增依赖
# ============================================================

# 向量数据库（嵌入式模式，无需外部服务）
chromadb>=0.5.0

# LangChain ↔ ChromaDB 集成
langchain-chroma>=0.2.0

# 文本嵌入模型（中文优化，本地运行）
sentence-transformers>=3.0.0

# 密码哈希（bcrypt）
passlib[bcrypt]>=1.7.4

# JWT 令牌签发 / 验证
python-jose[cryptography]>=3.3.0

# API 限流（FastAPI 集成）
slowapi>=0.1.9

# 压测引擎（异步 HTTP 客户端，用于服务端并发压测）
httpx>=0.27.0
